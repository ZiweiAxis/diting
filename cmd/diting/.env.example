# Diting 环境变量示例（复制为 .env 并填入真实值，.env 已在 .gitignore 中）
# 启动时自动加载 .env，无需再 export

# 飞书（必填才能收到飞书审批消息）
DITING_FEISHU_APP_ID=xxxx
DITING_FEISHU_APP_SECRET=***
DITING_FEISHU_APPROVAL_USER_ID=xxxx
# DITING_FEISHU_CHAT_ID=xxxx

# 可选：代理端口、CHEQ 超时等
# DITING_PROXY_LISTEN=:8080
# DITING_CHEQ_TIMEOUT_SECONDS=300
# CONFIG_PATH=config.acceptance.yaml
