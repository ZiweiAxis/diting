# Diting 私密/覆盖配置（复制本文件为 .env 并填入真实值）
# .env 不提交到仓库，仅本地使用；启动时自动加载，覆盖 config.yaml 中同名字段

# ---------- 飞书（必填才能收到审批消息） ----------
DITING_FEISHU_APP_ID=
DITING_FEISHU_APP_SECRET=
# 审批人：open_id 或 user_id（遇 99992361 时改用 user_id 并设 RECEIVE_ID_TYPE=user_id）
DITING_FEISHU_APPROVAL_USER_ID=
# 群聊兜底（可选）
# DITING_FEISHU_CHAT_ID=

# ---------- 飞书行为（可选，不设则用 config.yaml） ----------
# DITING_FEISHU_RECEIVE_ID_TYPE=user_id
# DITING_FEISHU_USE_CARD_DELIVERY=true
# DITING_FEISHU_USE_LONG_CONNECTION=true
# DITING_GATEWAY_BASE_URL=http://localhost:8080

# ---------- 大模型（main/main_feishu 用 config.yaml 时由 .env 覆盖） ----------
# DITING_LLM_BASE_URL=
# DITING_LLM_API_KEY=
# DITING_LLM_MODEL=
# DITING_LLM_PROVIDER=anthropic
# DITING_LLM_MAX_TOKENS=1024
# DITING_LLM_TEMPERATURE=0.7

# ---------- 其他覆盖（可选） ----------
# DITING_PROXY_LISTEN=:8080
# DITING_CHEQ_TIMEOUT_SECONDS=120
